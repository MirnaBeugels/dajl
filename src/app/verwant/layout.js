export default function Verwant({ children }) {
    return (
        <>
        <section>
            {children}
        </section>
        <section>
            Dit is de footer voor de verwant
        </section>
        </>
    )
}
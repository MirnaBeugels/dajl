export default function Patient({ children }) {
    return (
        <>
        <section>
            {children}
        </section>
        <section>
            Dit is de footer voor de patient
        </section>
        </>
    )
}